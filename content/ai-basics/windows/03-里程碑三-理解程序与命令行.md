---
title: 里程碑三：理解 AI 是怎样操作电脑的
provenance: 课程原文
maturity: 长青
content_type: 教程
reader_path: ai-basics
platform: windows
course_order: 03
---
> **阶段目标：** 打下四块概念地基——LLM、程序、副作用、命令行。这是全课唯一偏「理论」的一章，但每个概念都配着动手实验。
> **阶段成功标志：** 能用「输入 → 黑箱 → 输出」解释 LLM 和程序；能运行 Python 程序；能用命令操作文件；能运行一个批处理脚本。
> **⏱️ 预计总用时：** 90–120 分钟

> 💡 这一章学完，你就一只脚踏入了计算机科学的大门。这里挑选的是学 AI **必须**掌握的最小概念集：不教编程、不教数学、不教大模型原理。

---

## 3.1 LLM：一个输入话、输出话的黑箱

### 任务 1：建立黑箱模型

🎯 **成功标志：** 你能亲手画出「一段话 → LLM → 一段回答」，并指出哪边是输入、哪边是输出。

LLM 是大语言模型的简称。完全理解它很难，但直观理解很简单：

```text
输入一段话  ──→  ┌─────────┐  ──→  输出一段话
                │   LLM   │
                └─────────┘
```

它输出的话看起来有逻辑、像在回答你，简直和人一样。你平时用的豆包、DeepSeek，背后就是大语言模型。**先把它当黑箱**：不用管里面怎么运作，只看输入和输出。

**操作步骤**

1. 拿一张纸（或开个 Markdown 文件），画出上面的黑箱图。
2. 打开任意一个大模型对话工具，问三个不同类型的问题（一个知识题、一个创作题、一个判断题）。
3. 挑一条回答，找出一处「值得追问或核实」的地方，追问它。

🔍 **刚才发生了什么**
第 3 步是关键：LLM 的输出是概率生成的，**可能出错、可能编造**。「敢用 AI + 会核实」才是完整能力，缺一半都不行。

---

## 3.2 只做计算的程序

程序和 LLM 用同一个模型理解：

```text
输入  ──→  ┌─────────┐  ──→  输出
          │  程序    │
          └─────────┘
```

区别在于：程序的行为是确定的。加法程序，输入 1 和 2 一定输出 3；输入 100 和 200 一定输出 300。

### 任务 2：创建并运行 add.py

🎯 **成功标志：** 用至少三组不同数字运行 `add.py`，每次都输出正确的和。
⏱️ 预计用时：15 分钟

**操作步骤**

1. 在 WorkBuddy 中输入：

   ```text
   我正在学习程序的核心概念，先帮我准备好python运行环境，然后给我创建一个add.py文件，我输入两个数字，它就会输出这两个数字的和
   ```

2. 打开文件资源管理器确认 `add.py` 出现在课程文件夹里。
3. 在课程文件夹中打开终端（打开该文件夹后，按住 Shift 右键空白处 → 「在终端中打开」；或在资源管理器地址栏输入 `powershell` 回车），输入：

   ```bash
   python add.py
   ```

4. 看到「请输入第一个数字：」→ 输入一个数，回车 → 输入第二个数，回车 → 看到结果。
   > 📸 操作截图尚未补齐：终端中完整的一次加法运行过程
5. 重新输入 `python add.py`，换不同的数字**再跑两次**，验证每次都正确。

🔍 **刚才发生了什么**
你刚才「启动」了一个程序，给了它输入，收到了输出。程序跑完就退出了——所以要再算一次就要再启动一次。这个「启动 → 输入 → 输出 → 退出」的循环，就是一切程序的生命周期。

### 任务 3：举一反三——max.py 和其它运算

🎯 **成功标志：** `max.py` 用三组数据测试（最大数在开头、中间、结尾各一组），输出都正确；另外自选生成至少一个减法/乘法/除法程序并验证。

**操作步骤**

1. 在 WorkBuddy 中输入：

   ```text
   创建一个max.py文件，我输入五个数，它就会输出五个数中的最大值
   ```

2. 用 `python max.py` 运行，准备三组数据：
   - `9 1 2 3 4`（最大在开头）
   - `1 2 9 3 4`（最大在中间）
   - `1 2 3 4 9`（最大在结尾）
3. 自己出题：让 AI 再生成一个减法（或乘除法）程序，用两组数据验证。

> 💡 为什么要准备三组刁钻数据：这叫「边界测试」——程序在普通情况对，不代表在特殊情况也对。**会设计测试数据，是你验收 AI 工作成果的核心武器**，比会写代码更重要。

---

## 3.3 会产生副作用的程序

程序不仅能计算，还能**改变程序外面的世界**——创建文件、删除文件、发送消息。这类影响叫「副作用」（Side effect）。

### 任务 4：创建 create_folder.py

🎯 **成功标志：** 输入 `5`，文件夹里恰好新增 5 个子文件夹，同时程序输出完成提示。
⏱️ 预计用时：15 分钟

**操作步骤**

1. 在 WorkBuddy 中输入：

   ```text
   创建一个create_folder.py文件，输入一个数N，能创建N个文件夹
   ```

2. 终端运行：

   ```bash
   python create_folder.py
   ```

3. 输入 `5`，回车。
4. 打开文件资源管理器，**数一数**是不是恰好多了 5 个文件夹。
   > 📸 操作截图尚未补齐：文件资源管理器中新出现的 5 个文件夹
5. 指认三件事（对自己说出来）：
   - 输入是什么？→ 数字 N
   - 输出是什么？→ 「已创建 N 个文件夹」这句话
   - 副作用是什么？→ 电脑上**真实出现了** N 个文件夹

### 任务 5（挑战）：批量删除程序

🎯 **成功标志：** 运行前能数到刚创建的文件夹；运行后它们全部消失，**其它文件毫发无损**。

**操作步骤**

1. 让 WorkBuddy 写一个批量删除刚才那批文件夹的程序。
2. 运行前，先确认它删的是哪些文件夹（问 AI：「这个程序会删除哪些东西？会不会误删别的？」）。
3. 运行，然后打开文件资源管理器验证。

> ⚠️ 删除是最典型的「危险副作用」——错了无法撤销。**先看清会删什么，再运行。** 这个警觉性请保持一辈子。里程碑四的 Git 就是为这种时刻准备的后悔药。

🔍 **刚才发生了什么**
你现在理解了 Agent 的本质：**LLM（会说话的黑箱）+ 能产生副作用的程序 = 能操作电脑的 AI**。WorkBuddy 在里程碑一创建文件夹时，底层就是这么干的。

---

## 3.4 命令行：不用鼠标操作电脑

我们平时双击图标打开程序。但还有一种更强大的启动方式：**命令**。在鼠标发明之前，所有人都是用命令操作电脑的。

### 任务 6：用命令在文件夹之间移动

🎯 **成功标志：** `pwd` 的输出以课程文件夹名结尾；`ls` 能列出之前创建的 Python 文件。
⏱️ 预计用时：15 分钟

**操作步骤**

1. 右键开始按钮选「终端」，或按 Win 键搜索「终端」打开。你会看到一个等待输入的窗口。
2. 输入 `pwd`，回车。它会输出一串路径——`pwd` 的作用是告诉你「终端现在位于电脑的哪个文件夹」。
3. 输入 `ls`，回车。它会列出当前位置的文件和文件夹。
4. 进入课程文件夹：

   ```bash
   cd ~/Desktop/占占AI小白入门
   ```

   > 💡 偷懒技巧：输入 `cd `（带空格）后，把文件夹从文件资源管理器拖进终端，路径自动补全。
5. 再次输入 `pwd` 确认位置，再输入 `ls`，看到 `add.py`、`max.py` 就成功了。
   > 📸 操作截图尚未补齐：pwd + ls 的完整输出
**先记住这三个命令，它们是你在电脑里的腿：**

| 命令 | 作用 | 记法 |
|------|------|------|
| `pwd` | 我在哪 | print working directory |
| `ls` | 这里有什么 | list |
| `cd` | 去某个地方 | change directory |

### 任务 7：理解「程序名 + 参数」

🎯 **成功标志：** 能说出 `python add.py` 里哪个是程序、哪个是参数。

**操作步骤**

1. 运行 `python add.py`，完成一次加法。
2. 回答：`python` 是**被启动的程序**，`add.py` 是**交给它的参数**（告诉 python 要运行哪个文件）。

🔍 **刚才发生了什么**
命令行的语法就这么一条：`程序名 参数1 参数2 ...`。终端本身不干活，它只是接收你的命令、启动对应的程序。学会这一条，你就看得懂 90% 的命令了。

### 任务 8：不打开文件资源管理器，只用命令操作文件

🎯 **成功标志：** 最后 `ls` 能看到「命令行练习」文件夹，里面有「第一个文件.txt」和「修改名字后的文件.txt」。

**操作步骤**（每输入一条就 `ls` 看一眼变化）

```bash
pwd                                    # 确认在课程文件夹
mkdir 命令行练习                        # 创建文件夹
cd 命令行练习                           # 进入它
ni 第一个文件.txt                    # 创建空文件
cp 第一个文件.txt 第二个文件.txt         # 复制
mv 第二个文件.txt 修改名字后的文件.txt    # 改名
ls                                     # 验收
```

📸 截图 M3-8-1：全套命令执行后的终端画面

**新增四条命令，它们是你在电脑里的手：**

| 命令 | 副作用 |
|------|--------|
| `mkdir` | 创建文件夹 |
| `ni` | 创建文件 |
| `cp` | 复制文件 |
| `mv` | 移动文件 / 改名 |

---

## 3.5 批处理脚本：把一串命令打包成一个程序

单条命令只能做一个动作。把多条命令按顺序写进一个文件，就成了「批处理脚本」——一次执行一整套操作。

### 任务 9：让 AI 编写 create_project.bat

🎯 **成功标志：** 运行脚本后自动生成一套完整的项目文件夹结构；删掉再跑一次，能原样重建。
⏱️ 预计用时：15 分钟

**操作步骤**

1. 在 WorkBuddy 中输入：

   ```text
   请帮我创建一个 create_project.bat 批处理脚本。运行脚本后，自动创建一个名为"我的第一个AI项目"的文件夹，并在里面创建 资料、代码、图片、输出结果 四个文件夹和一个 README.md 文件。请先向我解释每一条命令，再创建脚本。
   ```

2. 在终端运行：

   ```bash
   .\create_project.bat
   ```

3. 验证是否出现这个结构：

   ```text
   我的第一个AI项目
   ├── README.md
   ├── 代码
   ├── 图片
   ├── 资料
   └── 输出结果
   ```

4. 打开脚本文件，找到里面的命令，至少指出两条并说出它们各创建了什么。
5. 删除「我的第一个AI项目」文件夹，再运行一次脚本，确认能原样重建。
   > 📸 操作截图尚未补齐：脚本两次运行后重建的目录结构
🔍 **刚才发生了什么——本章的大收束**
回忆黑箱模型：LLM 输入一段话、输出一段话。这段话可以是中文，**也可以是命令**。

> LLM 会输出命令 → 命令能启动程序 → 程序有副作用 → 副作用就是电脑操作。

这就是 AI 操作电脑的完整原理链。以前批处理脚本是程序员的专利；现在 AI 替你写脚本，你只需要**看得懂发生了什么、验得了收**。而这，正是你这一章练出来的能力。

🔬 **深入一层**
程序设计里有个概念叫「循环」：让计算机重复做一件事。要创建 100 个文件夹，鼠标点到手抽筋，循环只要三行。「重复的事情交给程序」是计算机思维的核心，也是你以后判断「这事该不该让 AI 写个脚本」的直觉来源。

---

## 里程碑三 · 毕业检查

- [ ] 我能画出 LLM 和程序的「输入 → 黑箱 → 输出」图
- [ ] `add.py`、`max.py` 都用多组数据验证过
- [ ] 我能说出 create_folder.py 的输入、输出、副作用分别是什么
- [ ] 我能不打开文件资源管理器，用 pwd/ls/cd/mkdir/touch/cp/mv 完成文件操作
- [ ] create_project.bat 删除重跑验证过
- [ ] 我能复述「AI 操作电脑的原理链」

**阶段交付物：** `add.py`、`max.py`、`create_folder.py`、删除脚本、`create_project.bat` 和由脚本生成的项目目录。

**下一站 →** [[ai-basics/windows/04-里程碑四-Git存档|里程碑四：用 Git 给 Agent 的修改建立存档]]
